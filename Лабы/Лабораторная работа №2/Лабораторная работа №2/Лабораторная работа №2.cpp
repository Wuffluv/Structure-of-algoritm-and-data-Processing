#include <iostream>
#include <fstream>
using namespace std;

int main() {
	setlocale(LC_ALL, "Russian");
	// Создаем переменные для хранения длин отрезков
	double a, b, c;
	// Запрашиваем данные у пользователя
	cout << "Введите длину первого отрезка: ";
	cin >> a;
	cout << "Введите длину второго отрезка: ";
	cin >> b;
	cout << "Введите длину третьего отрезка: ";
	cin >> c;
	// Открываем файл для записи результатов тестирования
	ofstream out("test_results.txt");
	// Проверяем, можно ли построить треугольник с заданными отрезками
	if ((a + b > c) && (b + c > a) && (c + a > b)) {
		// Вычисляем периметр треугольника
		double p = a + b + c;
		// Вычисляем площадь треугольника по формуле Герона
		double s = sqrt(p * (p - 2 * a) * (p - 2 * b) * (p - 2 * c)) / 2;
		// Определяем тип треугольника по длинам его сторон
		if ((a == b) && (b == c)) {
			// Результат тестирования
			out << "Тест пройден: заданные отрезки могут быть сторонами равностороннего треугольника" << endl;
			// Выводим результат на экран
			cout << "Заданные отрезки могут быть сторонами равностороннего треугольника" << endl;
		}
		else if ((a == b) || (b == c) || (c == a)) {
			// Результат тестирования
			out << "Тест пройден: заданные отрезки могут быть сторонами равнобедренного треугольника" << endl;
			// Выводим результат на экран
			cout << "Заданные отрезки могут быть сторонами равнобедренного треугольника" << endl;
		}
		else {
			// Результат тестирования
			out << "Тест пройден: заданные отрезки могут быть сторонами разностороннего треугольника" << endl;
			// Выводим результат на экран
			cout << "Заданные отрезки могут быть сторонами разностороннего треугольника" << endl;
		}
	}
	else {
		// Результат тестирования
		out << "Тест не пройден: заданные отрезки не могут быть сторонами треугольника" << endl;
		// Выводим результат на экран
		cout << "Заданные отрeзки не могут быть сторонами треугольника" << endl;
	}
	// Закрываем файл
	out.close();
	// Возвращаем ноль, чтобы показать, что программа завершена успешно
	return 0;
}